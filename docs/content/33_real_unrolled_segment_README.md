# 33_real_unrolled_segment.png

Place the real unrolled segment image here with this exact filename:

```text
docs/content/33_real_unrolled_segment.png
```

Use the attached unrolled segment image from the chat/PPT/export. This image should show the virtually flattened scroll surface after tracing, correction, and surface extension.

## Video Use

Use this image after the VC3D correction/surface extension section and before the model section.

## Caption

```text
Virtual unwrapping output: a curled 3D scroll surface converted into a flat reviewable segment.
```

## Voiceover

```text
After tracing and correcting the papyrus surface, the scroll can be virtually unwrapped into a flat segment like this. This step is critical because the ink model and later review interface need a usable 2D surface to work on.
```

